/*  This was for building the app with "r.js" and is not used/tested anymore.
 *  It will be replaced by a build with gulp, but remains here until then.
 *  to execute in windows: 'r.js.cmd -o src/scripts/build.js'
 *  note: all paths are relative to the one where build.js is located
 */

({
    baseUrl: '.',
    appDir: '../../src',
    dir: '../../dist',
    name: 'main'
})